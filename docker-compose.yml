version: '3.3'

services:
  app:
    build:
      context: .
    env_file: app/.env
    ports:
      - "9000:8000"
    volumes:
      - ./app:/app
      - ./version.txt:/app/version.txt:ro
    restart: unless-stopped
    stop_grace_period: 30s
